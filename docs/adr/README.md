# Architecture Decision Records

Code 0 的批准基线保存在 `docs/architecture/code-0/`，不得直接重写。

新决定复制 `ADR_TEMPLATE.md`，使用下一个编号，并通过架构审核。修改已冻结的 IPC Contract、SQLite 公共 Schema、Provider Protocol、Sidecar Protocol、公共 Job 语义或跨模块领域 Contract 前必须先提交 Architecture Question。
