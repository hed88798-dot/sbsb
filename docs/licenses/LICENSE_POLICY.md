# License Policy

本项目为闭源商业软件。依赖是否可进入发布物，以最终 artifact、传递依赖、模型权重和构建配置的实际许可证为准。

## 默认规则

- MIT、BSD、Apache-2.0、ISC 等宽松许可证可在保留通知并通过扫描后使用。
- LGPL 仅在动态链接、可替换、NOTICE、对应源码和其他义务均可履行时使用。
- GPL、AGPL、non-commercial、research-only、未知许可的代码、二进制、模型、字体或素材默认拒绝进入闭源安装包。
- FFmpeg 必须是可复现的 LGPL-only shared build，禁用 GPL/nonfree，且不含 libx264/libx265。
- Provider 平台条款、模型权重许可和输出权分别审查，付费 API 不自动代表可商用。

书面例外必须记录责任人、范围、依据和到期日，并通过架构与法务审核。
