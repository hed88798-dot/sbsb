# Gateway boundary

正式 Lightweight Backend 与 provider adapters 属于 Code B。本目录当前实现明确标记为 `NON_PRODUCTION / DEV_ONLY / REFERENCE_IMPLEMENTATION` 的 Mock Gateway，仅用于 Code A contract 与错误语义测试，不含授权、路由、成本、Provider Key 或厂商适配器。
